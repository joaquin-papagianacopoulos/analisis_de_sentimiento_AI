# Acá luego van las queries a MySQL.
# Dejamos funciones vacías para que el import no falle.

def fetch_news(conn, q, start, end, sentiments, page, page_size):
    # Implementarán esto cuando conecten la DB
    return 0, []
